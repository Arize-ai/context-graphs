{
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n api,ui -c blue,green \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd procurement-agent && uv run uvicorn src.main:app --reload --port 8000",
    "dev:ui": "cd procurement-agent/ui && npm run dev"
  }
}
