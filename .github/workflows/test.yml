name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  procurement-agent:
    name: procurement-agent (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: procurement-agent
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run tests
        run: uv run pytest -q

  procurement-reviewer:
    name: procurement-reviewer (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: procurement-reviewer
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run tests
        run: uv run pytest -q

  scripts:
    name: scripts (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scripts
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run tests
        run: uv run pytest -q

  procurement-ui:
    name: procurement-ui (vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: procurement-ui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: procurement-ui/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
