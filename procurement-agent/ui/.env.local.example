# Next.js auto-loads .env.local. Copy this file to .env.local and edit as needed.
#
#   cp .env.local.example .env.local
#
# Defaults work out of the box when the agent runs on localhost:8000.

# Base URL of the procurement-agent HTTP API. Defaults to http://localhost:8000.
# NEXT_PUBLIC_API_URL=http://localhost:8000
